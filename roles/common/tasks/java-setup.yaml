- name: installing maven
  ansible.builtin.dnf:
     name: "{{ item }}"
     state: present
  loop:
     - maven
     - mysql

- name: installing dependancies
  ansible.builtin.command: mvn clean package
  args:         
      chdir: /app